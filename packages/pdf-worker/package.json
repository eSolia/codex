{
  "name": "@esolia/pdf-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Shared PDF generation service for eSolia apps",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write ."
  },
  "dependencies": {
    "hono": "^4.11.10",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260305.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.63.0"
  }
}
